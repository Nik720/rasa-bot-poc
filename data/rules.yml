version: "2.0"

rules:

- rule: Say goodbye anytime the user says goodbye
  steps:
  - intent: goodbye
  - action: utter_goodbye

- rule: Say `hello` when the user start conversation with `greet` intent
  steps:
  - intent: greet
  - action: action_get_response
